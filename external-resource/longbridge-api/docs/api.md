API 参考
======

浏览并探索 Longbridge OpenAPI 的 HTTP 接口。在侧边栏选择一个接口，即可查看参数说明、请求示例和响应结构。

### HTTP API

本参考文档涵盖交易、行情和持仓管理等 HTTP 接口，所有接口均需要 OAuth 2.0 Bearer Token 认证。

### WebSocket / TCP API

实时行情推送、订单状态更新等实时数据通过 WebSocket 或 TCP 连接下发。

← 在侧边栏选择一个接口开始探索
